# In-cluster helper pod for driving the task-manager app's /stress/* endpoints.
# The app itself generates the load (CPU burn, memory fill, forced 503s); this
# pod just needs to reach the ClusterIP service, so a plain curl image is enough.
#
# Deploy:
#   kubectl apply -f kubernetes/07-stress-pod.yaml
#
# Trigger a scenario (service DNS resolves within the default namespace):
#   kubectl exec -n default stress-tester -- curl -s http://task-manager/stress/cpu
#   kubectl exec -n default stress-tester -- curl -s http://task-manager/stress/memory
#   kubectl exec -n default stress-tester -- curl -s http://task-manager/stress/crash
#   kubectl exec -n default stress-tester -- curl -s http://task-manager/stress/not-ready
#   kubectl exec -n default stress-tester -- curl -s http://task-manager/stress/reset
#
# Note: the app runs a single gunicorn worker (see app/Dockerfile), so one
# call to /stress/crash or /stress/not-ready is enough to flip its state.
#
# restartPolicy: Always -- this pod is not managed by a Deployment/ReplicaSet
# and is out of scope for the self-healing engine (which only watches
# TARGET_DEPLOYMENT), so if its container is ever killed (e.g. a minikube
# VM restart), kubelet's own native restart is what brings it back, not the
# AI. A bare Pod with restartPolicy: Never would otherwise be left dead
# permanently with nothing to recreate it.
apiVersion: v1
kind: Pod
metadata:
  name: stress-tester
  namespace: default
  labels:
    app: stress-tester
    purpose: load-generation
spec:
  restartPolicy: Always
  securityContext:
    runAsNonRoot: true
    runAsUser: 100
    seccompProfile:
      type: RuntimeDefault
  containers:
  - name: curl
    image: curlimages/curl:8.11.1
    imagePullPolicy: IfNotPresent
    # Keep the pod alive so it can be exec'd repeatedly.
    command: ["sleep", "infinity"]
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
        - ALL
    resources:
      requests:
        cpu: "10m"
        memory: "16Mi"
      limits:
        cpu: "50m"
        memory: "32Mi"
